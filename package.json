{
  "name": "moolah",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "yarn workspace backend build",
    "start": "yarn workspace backend start:prod",
    "dev": "yarn workspace backend start:dev & yarn workspace frontend start"
  },
  "dependencies": {
  }
}
